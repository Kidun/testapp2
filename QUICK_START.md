# Быстрый старт

## Запуск проекта (3 простых шага)

### 1. Запустите контейнеры

```bash
# Вариант 1: Используйте скрипт
./start.sh

# Вариант 2: Вручную
echo "AIRFLOW_UID=$(id -u)" > .env
docker-compose up -d
```

### 2. Дождитесь инициализации (2-3 минуты)

```bash
# Проверьте статус контейнеров
docker-compose ps

# Смотрите логи инициализации
docker-compose logs -f airflow-init
```

### 3. Откройте веб-интерфейс

```
URL: http://localhost:8080
Логин: airflow
Пароль: airflow
```

### 4. Активируйте DAG'и

В веб-интерфейсе найдите три DAG'а и включите их (переключатель слева):

- ✅ `data_collection_pipeline` (каждые 3 минуты)
- ✅ `hourly_analysis_pipeline` (каждый час)
- ✅ `error_analysis_pipeline` (каждые 2 часа)

## Что происходит?

### Сбор данных (каждые 3 минуты)

Система собирает данные из трёх источников:
1. 🌤️ **Погода** - температура, влажность, скорость ветра
2. 💱 **Валюты** - курсы USD, EUR, GBP, CNY
3. 💰 **Крипто** - цены BTC, ETH + фондовый индекс

Все данные сохраняются в базу `data/pipeline_data.db`

### Анализ данных (каждый час)

Система анализирует собранные данные:
- Считает статистики (среднее, медиана, и т.д.)
- Строит графики распределения
- Сохраняет в папку `data/hourly_analysis/`

### Анализ ошибок (каждые 2 часа)

Система проверяет качество работы:
- Анализирует время ответа API
- Считает количество ошибок
- ⚠️ Если ошибок больше 10 за 2 часа - останавливает пайплайн

## Где смотреть результаты?

### Через веб-интерфейс Airflow

1. Откройте http://localhost:8080
2. Кликните на название DAG
3. Смотрите графики задач и логи

### В файловой системе

```bash
# Зайдите в контейнер
docker-compose exec airflow-scheduler bash

# База данных
ls -lh /opt/airflow/data/pipeline_data.db

# Результаты почасового анализа
ls -lh /opt/airflow/data/hourly_analysis/

# Результаты анализа ошибок
ls -lh /opt/airflow/data/error_analysis/
```

### На вашем компьютере

Все файлы доступны в папке `data/`:
```
data/
├── pipeline_data.db          # База данных
├── hourly_analysis/          # Графики и статистики (каждый час)
└── error_analysis/           # Анализ ошибок (каждые 2 часа)
```

## Полезные команды

```bash
# Просмотр логов всех сервисов
docker-compose logs -f

# Просмотр только scheduler
docker-compose logs -f airflow-scheduler

# Проверка статуса контейнеров
docker-compose ps

# Перезапуск
docker-compose restart

# Остановка
docker-compose down

# Полная очистка (включая данные)
docker-compose down -v
rm -rf data/pipeline_data.db
```

## Ручной запуск DAG

Если не хотите ждать по расписанию:

```bash
# Запустить сбор данных
docker-compose exec airflow-scheduler airflow dags trigger data_collection_pipeline

# Запустить анализ
docker-compose exec airflow-scheduler airflow dags trigger hourly_analysis_pipeline

# Запустить проверку ошибок
docker-compose exec airflow-scheduler airflow dags trigger error_analysis_pipeline
```

## Тестирование компонентов

```bash
# Тест сбора данных из API
docker-compose exec airflow-scheduler python /opt/airflow/plugins/data_collectors.py

# Тест базы данных
docker-compose exec airflow-scheduler python /opt/airflow/plugins/database.py
```

## Решение проблем

### DAG'и не появляются в интерфейсе

```bash
# Перезапустите scheduler
docker-compose restart airflow-scheduler

# Проверьте ошибки в DAG'ах
docker-compose exec airflow-scheduler airflow dags list-import-errors
```

### Ошибки при сборе данных

- Проверьте интернет-соединение
- Посмотрите логи в веб-интерфейсе (кликните на задачу → Logs)
- API могут временно не работать - это нормально, система продолжит попытки

### Контейнеры не запускаются

```bash
# Пересоздайте контейнеры
docker-compose down -v
docker-compose up -d

# Если проблема с правами доступа
sudo chown -R $USER:$USER logs/ data/
```

## Требования задания

✅ Все требования выполнены:

**1.0 балл - Сбор данных**
- ✅ Каждые 3 минуты
- ✅ Из 3 источников параллельно
- ✅ 10+ колонок данных
- ✅ Запись времени и ошибок в БД

**0.3 балла - Почасовой анализ**
- ✅ Базовые статистики
- ✅ Графики распределения
- ✅ Отдельная папка для результатов

**0.4 балла - Анализ ошибок**
- ✅ Каждые 2 часа
- ✅ Анализ времени ответа
- ✅ Графики и описание
- ✅ Остановка при >10 ошибках

**0.1 балл - База данных**
- ✅ SQLite в файле
- ✅ SQLAlchemy ORM
- ✅ Контекстные менеджеры

## Демонстрация работы

Для полной демонстрации:

1. Запустите проект и оставьте работать на **2-3 часа**
2. За это время накопится достаточно данных для анализа
3. Посмотрите результаты в папках `data/hourly_analysis/` и `data/error_analysis/`
4. В веб-интерфейсе покажите выполненные задачи (зелёные квадратики)

## Дополнительно

### Увеличить частоту сбора

Отредактируйте `dags/data_collection_dag.py`:
```python
schedule_interval='*/1 * * * *',  # Каждую минуту вместо 3
```

### Изменить порог ошибок

Отредактируйте `dags/error_analysis_dag.py`:
```python
ERROR_THRESHOLD = 5  # Вместо 10
```

### Использовать PostgreSQL (дополнительно +0.2 балла)

PostgreSQL уже настроен в `docker-compose.yml` для Airflow метаданных. Чтобы использовать его для данных:

1. Измените в `docker-compose.yml`:
```yaml
DATABASE_URL: 'postgresql://airflow:airflow@postgres/airflow'
```

2. Обновите `requirements.txt`:
```
psycopg2-binary==2.9.9
```

3. Пересоберите образ:
```bash
docker-compose down
docker-compose build
docker-compose up -d
```

## Поддержка

При возникновении проблем:
1. Проверьте логи: `docker-compose logs -f`
2. Проверьте статус: `docker-compose ps`
3. Перечитайте README.md для подробной информации

Удачи с домашним заданием! 🚀
